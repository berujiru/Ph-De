# GEMINI.md

See [`AGENTS.md`](AGENTS.md) for full project guidance (stack, docs to read,
commands, architecture) — that file is the canonical source, shared across
AI coding tools. Read it before making non-trivial changes.

This repo has no Gemini-specific tooling beyond that; the "Tool-specific
notes" section at the end of `AGENTS.md` covers how to follow the same role
boundaries that Claude Code's subagents (`.claude/agents/`) encode, without
needing an equivalent subagent mechanism.
